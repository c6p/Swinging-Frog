import Koji from '@withkoji/vcc';
import { CONFIG } from '../config';

export class GameScene extends Phaser.Scene {

  constructor() {
    super({
      key: 'GameScene'
    })
  }

  init(data) { }

  preload() { }
  
  create() { }

  update(t,dt) { }

}